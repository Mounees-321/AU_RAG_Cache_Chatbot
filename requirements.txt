# ---------------------------------------------------------------------------
# Anna University IT Dept RAG Chatbot + Redis Semantic Caching
# One merged, conflict-resolved requirements file for the whole project.
# ---------------------------------------------------------------------------

# --- Ingestion / chunking ---------------------------------------------------
requests>=2.31.0
beautifulsoup4>=4.12.0
langchain-text-splitters>=0.2.0

# --- Vector store / retrieval / re-ranking ----------------------------------
chromadb>=0.4.22
sentence-transformers>=2.5.0

# --- Generation (LLM) --------------------------------------------------------
groq>=0.4.0

# --- Agentic web-search fallback ---------------------------------------------
ddgs>=9.0.0

# --- Semantic caching (Redis + RedisVL) --------------------------------------
# Sits in front of the RAG pipeline; a cache hit skips retrieval, re-ranking,
# and the Groq call entirely. Graceful-degrade if Redis is unreachable — see
# semantic_cache.py.
redis>=5.0.0,<7.0.0
redisvl>=0.8.0,<0.9.0

# --- Config -------------------------------------------------------------
python-dotenv>=1.0.0

# --- API layer ----------------------------------------------------------
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0

# --- Frontend -------------------------------------------------------------
streamlit>=1.32.0

# --- Testing -------------------------------------------------------------
pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0
